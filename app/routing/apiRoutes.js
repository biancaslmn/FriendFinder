// =============================================================
// Create a set of routes that then display matches data as JSONs
// =============================================================

app.get("/api/friends", function(req, res) {
    return res.json(friends);
  });
  
  app.get("/api/friends", function(req, res) {
    return res.json(friends);
  });